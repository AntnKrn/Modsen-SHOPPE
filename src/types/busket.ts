export type busketType = {
  idProduct: number;
  quantity: number;
};

export type busketDeleteItemType = {
  uuid: string;
  id: number;
};

export type busketProdutType = {
  id: number;
  image: string;
  title: string;
  price: number;
  quantity: number;
};
